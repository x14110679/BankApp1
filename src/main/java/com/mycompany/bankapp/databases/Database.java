package com.mycompany.bankapp.databases;

import com.mycompany.bankapp.models.Account;
import com.mycompany.bankapp.models.Customer;
import java.util.ArrayList;
import java.util.List;


public class Database {        }                    // delegated implementation
  
    /*
     public static List<Customer> customerDB = new ArrayList<>();
     public static List<Account> accountDB = new ArrayList<>();
     public static boolean init = true;
     
     public Database() {
         if(init){
             Account acc1 = new Account (1, "One account");
             Account acc2 = new Account (2, "Another account");
             accountDB.add(acc1);
             accountDB.add(acc2);
             Customer cu1 = new Customer(1, "Manuel", "Fernandez", "JackEmployee", "manuel@gmail.com", "Dublin 1", accountDB);
             customerDB.add(cu1);
             init = false;
         }
     }
    public static List<Customer> getCustomersDB(){ 
        return customerDB;
    }
   
    public static List<Account> getAccountsDB() { 
        return accountDB;
    }
}
*/
// plus get not set